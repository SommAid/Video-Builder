# Video-Builder

Using pre-trained text-video, image-image, image-text, and other models develop a full length (21 minute) episode. 

The plan is to use models from hugging face and google colab to run the models and generate the content.
